---
description: Exemple de slash command — duplique ce fichier et adapte
---

# Titre de la commande

Décris ce que fait la commande. Le corps de ce fichier est injecté comme
prompt quand l'utilisateur tape `/example`.

## Steps

1. Ce que Claude doit faire, étape par étape.
2. Commande(s) shell à lancer, ex : `ssh -i <SSH_KEY> user@<HOST_IP> "..."`.
3. Format du rapport attendu.
